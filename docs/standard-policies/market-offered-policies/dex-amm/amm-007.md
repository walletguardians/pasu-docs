---
description: Swap Above USD Cap (WARN)
---

# AMM-007: 스왑 금액이 기준을 초과할 시 경고

### Policy Definition (정책 정의)

> 한 번에 교환하는 토큰 금액이 기준을 넘는 경우, 규모를 다시 확인하도록 경고합니다.

금액을 잘못 입력했거나, 거래량이 얕은 풀로 큰 물량을 밀어 넣어 가격이 크게 밀리거나, MEV 봇에 앞뒤로 끼이면 받는 양이 기대보다 줄어들 수 있습니다. 넣는 토큰을 시세로 환산한 값이 기준을 넘으면, 의도된 교환인지 서명 전에 한 번 더 따져보도록 경고합니다.

#### Scope (적용 범위)

DEX 스왑. 넣는 토큰과 수량을 시세로 환산했을 때 기준을 넘는 경우에 적용됩니다.

#### Audience (대상 사용자)

DEX에서 큰 금액을 한 번에 교환하는 사용자

#### Used Data (판정에 사용될 데이터)

투입 토큰의 USD 환산 가치

#### Policy in Code

{% code title="policy.cedar" %}
```solidity
@id("swap-usd-cap-warn")
@severity("warn")
@reason("고액 스왑입니다(기준 초과) — 규모를 확인하세요")
forbid(principal, action == Amm::Action::"Swap", resource)
when {
  context has custom
  && context.custom has swapUsd
  && context.custom.swapUsd.greaterThan(decimal("25000.0000"))
};
```
{% endcode %}

{% code title="manifest.json" %}
```json
{
  "id": "swap-usd-cap-warn",
  "schema_version": 2,
  "trigger": { "where": { "action.tag": { "eq": "swap" } } },
  "policy_rpc": [{
    "id": "swap-in-usd",
    "method": "oracle.usd_value",
    "params": {
      "chain_id": "$.root.chain_id",
      "asset": "$.action.tokenIn",
      "amount": "$.action.direction.amountIn"
    },
    "outputs": [{ "kind": "context", "field": "swapUsd", "type": "Decimal", "from": "$.result.usd", "required": false }],
    "optional": true
  }],
  "custom_context": { "fields": { "swapUsd": "decimal" } }
}
```
{% endcode %}

***

**AMM-007: 스왑 금액이 기준을 초과할 시 경고**\
Wallet Guardians | v.1.0.0 | 26/06/13\
\
&#xNAN;_&#x53;upported Chain: Ethereum_
