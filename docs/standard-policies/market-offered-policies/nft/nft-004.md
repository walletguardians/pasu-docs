---
description: NFT Transfer to a Blocklisted Address (DENY)
---

# NFT-004: 블랙리스트에 오른 주소로 NFT를 전송할 시 차단

### Policy Definition (정책 정의)

> 받는 주소가 알려진 드레이너·제재 **블록리스트**에 올라 있으면, NFT 전송이 나가지 않도록 **차단**합니다.

NFT를 전송하면 소유권이 받는 주소로 그대로 넘어가고, 한 번 나간 전송은 되돌릴 수 없습니다. 받는 주소가 제재 목록에 오른 주소라면, NFT를 보내는 순간 그대로 빼앗기게 됩니다. 그래서 받는 주소가 블랙리스트에 올라 있으면 전송 자체를 막습니다.

#### Scope (적용 범위)

NFT 전송에 적용됩니다. 받는 주소가 블랙리스트에 등재된 경우입니다.

#### Audience (대상 사용자)

NFT를 보유하거나 전송하는 모든 사용자

#### Used Data (판정에 사용될 데이터)

* 받는 주소와 체인 ID(`$.root.chain_id`)&#x20;
* 블랙리스트

#### Policy in Code

{% code title="policy.cedar" %}
```solidity
@id("nft-transfer-blocklisted-recipient-deny")
@severity("deny")
@reason("받는 주소가 알려진 드레이너/제재 블록리스트에 있습니다 — NFT 전송을 차단합니다. 주소를 다시 확인하세요.")
forbid(principal, action == Token::Action::"NftTransfer", resource)
when {
  context has custom
  && context.custom has recipientFlagged
  && context.custom.recipientFlagged
};
```
{% endcode %}

{% code title="manifest.json" %}
```json
{
  "id": "nft-transfer-blocklisted-recipient-deny",
  "schema_version": 2,
  "trigger": {
    "where": {
      "action.tag": {
        "eq": "nft_transfer"
      }
    }
  },
  "policy_rpc": [
    {
      "id": "recipient-reputation",
      "method": "address.reputation",
      "params": {
        "chain_id": "$.root.chain_id",
        "address": "$.action.recipient"
      },
      "outputs": [
        {
          "kind": "context",
          "field": "recipientFlagged",
          "type": "Bool",
          "from": "$.result.flagged"
        }
      ],
      "optional": true
    }
  ],
  "custom_context": {
    "fields": {
      "recipientFlagged": "Bool"
    }
  }
}
```
{% endcode %}

***

**NFT-004: 블록리스트에 오른 주소로 NFT를 전송할 시 차단**\
Wallet Guardians | v.1.0.0 | 26/06/13\
\
\&#xNAN;_Supported Chain: Ethereum_
